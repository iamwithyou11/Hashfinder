DESTDIR ?= /usr/local/bin

install:
	@sudo cp hash.py $(DESTDIR)/Hashfinder
	@sudo chmod +x $(DESTDIR)/Hashfinder
	@echo "Installation Successful!"

uninstall:
	@sudo rm -f $(DESTDIR)/Hashfinder
	@echo "Hashfinder has been removed"
